# /goal：彻底解决本地 VideoCompress 项目重建视频时序抖动、ROI 闪烁和过期参考问题

## 0. 项目范围与执行边界

本项目代码已经存在于本地工作区。

**只分析和修改当前本地项目。**

不要访问、拉取、修改或同步线上 GitHub 仓库。

禁止执行：

```text
git clone
git pull
git fetch
git push
git reset --hard origin/*
git checkout origin/*
```

不要假设线上 GitHub 版本比本地代码更新。

不要用线上仓库覆盖本地工作区。

本地项目可能已经存在：

```text
未提交修改
实验性修改
尚未 push 的修复
与远程 main 不一致的代码
```

这些都是正常情况。

**当前本地工作区才是唯一事实来源。**

可以使用：

```bash
git status
git diff
git diff --cached
git log
```

检查本地修改历史和当前状态。

但不要进行任何可能覆盖用户现有工作区的 Git 操作。

尤其禁止：

```bash
git reset --hard
git clean -fd
git checkout -- .
git restore .
```

除非用户明确要求。

---

# 1. Agent 工作要求

请直接检查当前本地项目实际代码并完成修改。

不要：

```text
读取本 goal
→ 回复“已读取目标文件”
→ 停止
```

读取目标后必须立即继续：

```text
分析代码
→ 找出实际实现
→ 判断已完成/未完成项
→ 修改代码
→ 编写测试
→ 运行测试
→ 更新文档
→ 审查 diff
```

直到当前本地环境能够完成的工作全部完成。

---

# 2. 总体目标

彻底解决 reconstruction / ROI 重建模式中的肉眼抖动和闪烁。

重点解决：

```text
ROI ↔ BASE 周期切换

ROI-LANCZOS ↔ ROI-ESRGAN 跳变

旧参考图跟随运动物体造成的拖影和抖动

PATCH 已完整收到但仍等待下一条 STATE

过期 reference 继续显示

过期 reference 继续进入 Real-ESRGAN

SR 异步完成后修改同一个 H.265 source frame

reference_generation 新旧代倒退

UDP 乱序导致旧 reference 重新生效

bbox 微小变化导致 SR 重算

reference refresh 周期大于 reference 内容有效期导致周期 BASE gap
```

最终原则：

> 新 reference 尽早生效，旧 reference 严格退出；同一个 source video frame 一旦完成重建，在整个显示周期中保持像素不变；所有异步结果只能影响后续 source frame。

---

# 3. 先检查本地现状，不要假设 fix 已经完成

首先检查当前本地代码。

重点阅读实际存在的：

```text
tools/rebuild_receiver.py
tools/live_h265_hud.py
tools/test_rebuild_protocol.py

cpp/common/config.cc
cpp/common/config.h
```

以及实际负责：

```text
PATCH/reference 生成
PATCH/reference 分片
PATCH/reference FEC
STATE 发送
reference refresh
网络限速/token bucket
H265 发送调度
```

的 C/C++ 文件。

同时阅读：

```text
fix.md
启动.md
协议.md
README.md
```

如果文件名称或目录发生变化，根据代码搜索实际实现。

不要因为路径不同而跳过对应逻辑。

---

# 4. 开始修改之前输出内部检查结果

先根据本地代码把目标分成：

```text
已实现
部分实现
未实现
实现存在问题
```

至少检查：

```text
PATCH local-ready

450ms content hard age

1000ms cache TTL

100ms future gate

generation/reference_generation 防倒退

Real-ESRGAN warm-up

SR 单 worker

latest-only pending

SR submit 前 age check

SR finish 后 age/generation check

source frame compose cache

same-source-frame immutable

bbox EMA

aspect-ratio resize

matchTemplate 配准

稳定 SR cache key

sender reference refresh
```

然后直接继续修改。

不需要等待用户确认。

---

# 5. 已有正确机制尽量保留

如果本地当前代码已经实现：

```text
解码 PTS FIFO
PTS bias / offset 中位数估计
有限 PTS 外推
future semantic gate
generation 检查
bbox EMA
等比缩放
matchTemplate 内容配准
稳定 SR cache key
Real-ESRGAN 后台 warm-up
```

不要推翻。

本次重点是在现有机制上补齐：

```text
temporal consistency
reference lifecycle
SR lifecycle
source-frame immutability
sender refresh deadline
```

---

# 6. 核心修改一：拆分 Reference 时间概念

禁止继续用单个：

```text
reference_max_age
```

同时表达：

```text
内存缓存寿命
内容有效期
PTS 匹配范围
```

至少拆成：

```text
REFERENCE_CACHE_TTL_MS = 1000

REFERENCE_CONTENT_HARD_MAX_AGE_MS = 450

REFERENCE_FUTURE_MAX_MS = 100

STATE_SYNC_MAX_MS = 100
```

具体命名遵守项目现有风格。

---

# 7. CACHE TTL

```text
REFERENCE_CACHE_TTL_MS = 1000
```

只决定 reference 对象何时从 PC 内存中删除。

它可以继续保留：

```text
1000ms
```

但：

> reference 还在 cache 中，不等于 reference 可以继续显示。

严禁：

```text
cache TTL = display validity
```

---

# 8. 内容硬时效

计算：

```text
content_age_ms =
current_video_pts_ms
-
reference_source_pts_ms
```

要求：

```text
content_age <= 450ms
```

才有资格进行 ROI 重建。

超过：

```text
450ms
```

必须：

```text
BASE
AGEDROP++
```

不得因为：

```text
还没收到新 reference
希望画面连续
SR 已经完成
reference 仍存在 cache
```

而继续使用旧 reference。

---

# 9. Future gate

如果：

```text
reference_pts - video_pts > 100ms
```

必须 fail closed。

例如：

```text
+99ms
```

其他条件满足时允许。

```text
+101ms
```

必须拒绝。

---

# 10. 核心修改二：PATCH 完成后立即 local-ready

当 reference 已完成：

```text
FEC OK
CRC OK
JPEG OK
mask OK
generation valid
reference_generation valid
```

PC 立即：

```text
local_ready = true
```

不再等待：

```text
下一条 STATE ready flag
```

才能确认 reference 已存在。

推荐 key：

```text
generation
track_id
reference_generation
```

或者保持本项目已有等价 key。

---

# 11. PATCH 和 STATE 职责解耦

PATCH/reference 负责：

```text
reference 图像内容
mask
source PTS
generation
reference_generation
完整性校验
```

完整 PATCH：

```text
=> local reference ready
```

STATE 负责：

```text
track 是否存在
bbox
track 状态
当前 generation
当前 reference_generation
几何位置
```

STATE 不应重新否认已经完整收到的 PATCH。

尤其禁止：

```text
new PATCH ready
       ↓
late old STATE
       ↓
reference 重新 not-ready
```

---

# 12. generation/reference_generation 必须防倒退

以下所有路径都检查：

```text
PATCH
STATE
SR candidate
SR pending
SR result
SR cache
render selection
```

原则：

```text
new generation wins
old generation never overwrites new generation
```

例如当前：

```text
reference_generation = 25
```

后来收到：

```text
reference_generation = 24
```

直接 drop。

绝不能：

```text
25 → 24
```

---

# 13. 核心修改三：SR 两次时效检查

Real-ESRGAN 是异步过程。

必须至少存在两个检查点。

## A. 真正提交 worker 前

```python
age = current_video_pts - ref.source_pts
```

如果：

```text
age > 450ms
```

则：

```text
不执行 SR
X++
```

如果：

```text
reference future >100ms
```

同样拒绝。

---

# 14. SR 完成后再次检查

SR result 完成不能直接进入有效融合路径。

重新检查：

```text
generation
reference_generation
reference age
future offset
track validity
```

如果 SR 开始时：

```text
age = 200ms
```

但完成时：

```text
age = 700ms
```

结果必须废弃。

如果 SR 推理期间：

```text
refgen 20 → 21
```

旧：

```text
refgen 20 SR result
```

必须：

```text
STALE++
drop
```

---

# 15. SR scheduler：1 RUNNING + 1 latest pending

保持或实现：

```text
最多一个 running
最多一个 pending
```

pending 只保存：

```text
latest candidate
```

例如：

```text
A running
B pending
C arrives
```

变成：

```text
A running
C pending
B discard
```

A 完成以后不能直接运行 C。

必须重新检查：

```text
C content_age
C generation
C reference_generation
```

如果 C 已经：

```text
age >450ms
```

则：

```text
X++
```

不执行 Real-ESRGAN。

---

# 16. 保留 SR warm-up

Real-ESRGAN 首次 CUDA 初始化应后台 warm-up。

不能阻塞：

```text
UDP receiver
H265 decode
display
STATE/PATCH receiver
```

SR 尚未 ready 时：

```text
ROI-LANCZOS
```

可以正常工作。

不要把：

```text
ESRGAN ready
```

作为 ROI 显示的必要条件。

---

# 17. 核心修改四：同一个 source frame 只能 compose 一次

这是解决肉眼抖动的 P0 项。

假设：

```text
H265 = 6fps
display = 12fps
```

不能：

```text
source #100

tick 1
ROI-LANCZOS

SR 完成

tick 2
ROI-ESRGAN
```

因为虽然 source frame 没变：

```text
画面像素已经改变
```

这会直接形成局部闪烁。

---

# 18. 建立 source_sequence

对真正解码获得的新 H265 frame 建立：

```text
source_sequence
```

如果已有：

```text
frame_id
decode_sequence
source_seq
```

优先复用可靠字段。

逻辑：

```python
if source_sequence != last_composed_source_sequence:

    composed_frame = composer.render(...)

    last_composed_source_sequence = source_sequence

output(composed_frame)
```

同一个 source sequence 被 display 多次：

```text
直接重复 composed_frame
```

不能重新 compose。

---

# 19. Source frame 一旦 compose 就 immutable

异步发生：

```text
PATCH 到达
STATE 到达
SR 完成
bbox 更新
reference generation 更新
```

都不能改变已经 compose 的当前 source frame。

例如：

```text
source #100
```

已经输出：

```text
ROI-LANCZOS
```

随后 SR 完成。

source #100 仍必须保持：

```text
ROI-LANCZOS
```

到：

```text
source #101
```

才允许：

```text
ROI-ESRGAN
```

原则：

> ASYNC RESULT NEXT FRAME ONLY。

---

# 20. 建议采用不可变 RebuildSnapshot

每个新 source frame 到来时生成：

```text
RebuildSnapshot

source_sequence
video_pts
state_generation
reference_generation
bbox
selected_reference
reference_pts
selected_sr_result
render_mode
```

该 snapshot 一旦建立：

```text
当前 source frame 生命周期内不可修改
```

后台 thread 只能更新：

```text
next frame 所需状态
```

不能修改当前 composed frame。

---

# 21. 核心修改五：检查 sender reference refresh

这是不能只修改接收端的部分。

检查本地 sender 当前：

```text
reference refresh interval
reference capture interval
reference queue
PATCH packet scheduling
FEC
token bucket
rate limiter
```

如果仍然类似：

```text
refresh = 700ms
```

而 receiver：

```text
hard age = 450ms
```

则天然产生：

```text
ROI
↓
450ms
BASE
↓
下一张 reference
ROI
```

形成周期闪烁。

必须解决。

---

# 22. 不要简单改成固定 200/250ms

项目是低带宽视频传输。

不能：

```text
reference 刷新率暴增
→ 抢占 H265
→ 视频延迟上升
→ 解码断流
```

应该采用：

# bandwidth-aware deadline-driven refresh

目标不是：

```text
每隔固定 N ms 发 reference
```

而是：

> 尽可能让下一代 reference 在当前 reference 的 450ms hard deadline 之前完成传输。

---

# 23. Sender refresh 推荐参数

可以增加：

```text
REFERENCE_SOFT_REFRESH_MS

REFERENCE_HARD_DEADLINE_MS

REFERENCE_REFRESH_GUARD_MS
```

初始值可考虑：

```text
SOFT_REFRESH = 200~250ms

HARD_DEADLINE = 450ms

GUARD = 50~100ms
```

但不要未经分析直接固定。

必须结合：

```text
链路限速
reference 平均大小
FEC overhead
PATCH chunk 数
H265 实际占用
发送 queue delay
```

确定。

---

# 24. 优先根据预计传输耗时触发下一代

如果可以估算：

```text
encode time
queue delay
reference transmission time
```

则：

```text
next reference start
≈
hard deadline
-
estimated delivery
-
guard
```

例如：

```text
hard = 450ms
estimated delivery = 140ms
guard = 60ms
```

则大约：

```text
250ms
```

就应启动下一张 reference。

目标：

```text
reference N+1 ready
<
reference N hard expire
```

---

# 25. Sender telemetry

至少记录：

```text
reference_generation

capture time

encode finish time

queue enter time

first packet send time

last packet send time

reference bytes

chunk count

FEC bytes

queue delay
```

PC 端如果有：

```text
source timestamp
complete receive timestamp
```

则增加：

```text
reference delivery latency
```

建议统计：

```text
REFLAT current
REFLAT p50
REFLAT p95
```

---

# 26. 优化 reference 大小

如果 reference 经常无法在 450ms 生命周期内送达：

优先考虑动态控制：

```text
JPEG quality
reference resolution
ROI downscale
mask size
mask encoding
FEC overhead
```

目标是：

```text
低延迟完成 reference
```

而不是：

```text
reference JPEG 尽可能高清
```

设计优先级：

```text
temporal correctness
>
reference image quality
```

---

# 27. 尽量减少 reference 跨多帧发送

统计当前：

```text
reference 平均 bytes
reference p95 bytes
每个 source frame 可发送 PATCH bytes
```

如果一张 reference 经常跨：

```text
2~3 个 source frame
```

才发完：

需要分析能否通过：

```text
压缩
调整 chunk scheduling
动态 JPEG quality
动态 reference resolution
```

减少 capture→complete 延迟。

但：

> 不能突破总链路限速。

---

# 28. 无法及时获得新 reference 时正确 fallback

低带宽时可能确实出现：

```text
old ref age = 450ms
new ref 未完整到达
```

正确行为：

```text
ROI
↓
BASE
↓
new ref ready
↓
ROI
```

而不是：

```text
继续使用 800~1000ms old ROI
```

正确性优先。

sender 调度则尽量缩短 BASE gap。

---

# 29. 几何稳定机制继续保留

确认：

```text
bbox EMA
aspect ratio preserve
content registration
matchTemplate
match score threshold
```

仍然有效。

不要为了本次时序修改删掉。

---

# 30. SR cache key 必须稳定

cache key 不应包含频繁变化的：

```text
bbox width
bbox height
output resize width
output resize height
```

推荐：

```text
generation
track_id
reference_generation
SR profile/model
```

bbox 从：

```text
238x141
239x140
240x142
```

变化时：

不能触发三次独立 ESRGAN。

SR 只针对：

```text
reference content
```

最终 target resize 在 compose 阶段完成。

---

# 31. 可选：450ms 前渐隐

这一项是 P2。

不能代替 hard age。

建议：

```text
0~300ms:
alpha = 1

300~450ms:
alpha 逐渐 1→0

>=450ms:
alpha = 0
```

例如：

```python
if age <= fade_start:
    alpha = 1.0
elif age < hard_age:
    alpha = (hard_age - age) / (hard_age - fade_start)
else:
    alpha = 0.0
```

如果真实视觉测试发现 fade 带来：

```text
ghosting
```

则默认关闭。

但任何情况下：

```text
450ms 之后 ROI 必须完全退出。
```

---

# 32. HUD 增强

至少显示：

```text
REF valid/total

PTSAGE +xxx/450ms

AGEDROP n

GEN current/reference

REGDROP n

ROI AREA xx%

SR backend

SR RUN
SR Q
SR DONE
SR X
SR STALE
```

定义：

```text
AGEDROP
reference >450ms 导致 reject

Q
pending 数量，只能 0/1

X
SR 启动前发现 reference 已过期

STALE
SR 完成后发现 generation/age 已失效
```

---

# 33. HUD mode 保持语义准确

明确区分：

```text
BASE-LANCZOS

ROI-LANCZOS

ROI-ESRGAN
```

不能出现：

```text
MODE=ROI
ROI AREA=0%
```

这类明显语义冲突。

---

# 34. Timing debug 日志

建议提供可配置：

```text
--rebuild-debug-timing
```

开启后每个 source frame 输出结构化记录：

```text
SEQ=1234
VPTS=38120
SPTS=38105
RPTS=37970
AGE=150
GEN=8
RGEN=27
READY=LOCAL
MODE=ROI-ESRGAN
MATCH=0.91
SR=HIT
```

默认不要输出大量 debug 信息。

---

# 35. 必须新增测试：449/450/451ms

分别测试：

```text
449ms
```

允许 ROI。

```text
451ms
```

必须：

```text
BASE
AGEDROP +1
ROI AREA = 0
```

450ms 边界统一决定：

```text
<=450
```

或：

```text
<450
```

但整个工程必须一致。

推荐：

```text
<=450ms valid
>450ms invalid
```

---

# 36. local-ready 测试

场景：

```text
PATCH complete

CRC/FEC/JPEG/mask OK

没有新的 ready STATE
```

必须：

```text
local_ready=true
```

如果：

```text
generation
PTS
track
geometry
```

全部有效：

允许 ROI。

---

# 37. late STATE 测试

当前：

```text
refgen=25
PATCH ready
```

随后收到：

```text
STATE refgen=24
```

必须：

```text
drop
```

当前 refgen 仍：

```text
25
```

---

# 38. SR latest-only 测试

```text
A running
B pending
C arrives
```

结果：

```text
A running
C pending
B discarded
```

并验证：

```text
Q <=1
```

---

# 39. SR expired-before-submit 测试

```text
A running
C pending
```

A 完成时：

```text
C age=500ms
```

必须：

```text
C 不执行 SR
X++
```

---

# 40. SR stale-after-compute 测试

SR 开始：

```text
generation=10
refgen=20
```

执行过程中当前切换：

```text
generation=10
refgen=21
```

旧 SR 完成：

```text
refgen=20
```

必须：

```text
drop
STALE++
```

---

# 41. 最重要测试：same-source-frame immutable

创建：

```text
source_sequence=100
```

第一次 compose：

```text
ROI-LANCZOS
```

然后模拟：

```text
SR 完成
```

但仍然：

```text
source_sequence=100
```

第二个 output tick：

> 输出图像必须与第一次完全一致。

最好直接做：

```text
pixel equality
```

而不是只比较 mode。

直到：

```text
source_sequence=101
```

才允许使用新的 SR result。

---

# 42. Reference rollover 测试

```text
reference #20
age=300ms

reference #21 arrives
```

正确：

```text
20 → 21
```

如果 #21 已经 ready，不应：

```text
20 → BASE → 20 → 21
```

---

# 43. UDP reorder 测试

模拟：

```text
generation 30 PATCH/STATE
```

随后晚到：

```text
generation 29 PATCH/STATE
```

最终仍：

```text
generation 30
```

---

# 44. PATCH 丢包测试

如果 PATCH 无法通过：

```text
FEC
CRC
JPEG
mask
```

任意必要校验：

不得：

```text
local_ready
```

更不能显示损坏 reference。

---

# 45. 软件 jitter 测试

至少模拟：

```text
0ms
20ms
50ms
100ms
```

网络 jitter。

观察：

```text
PTS DROP
AGEDROP
generation drop
STATE drop
ROI/BASE transition
```

尤其确认：

```text
packet reorder
```

不会让 generation 倒退。

---

# 46. Real-ESRGAN 验证

如果本地机器支持 CUDA，并且模型存在：

使用真实 Real-ESRGAN 验证：

```text
warm-up 不阻塞 receiver

running <=1

pending <=1

expired candidate 不执行

stale result 不融合

same source frame 不因 SR 完成变化
```

如果环境没有 CUDA：

可以完成 mock test。

但最终必须明确：

```text
Real-ESRGAN CUDA integration 未验证
```

不能伪造验证结果。

---

# 47. 测试现有代码

修改以后必须运行现有测试。

优先使用项目当前已有测试入口。

例如：

```bash
python tools/test_rebuild_protocol.py
```

或者：

```bash
pytest
```

根据实际工程确定。

所有原有测试原则上都必须继续通过。

如果原测试本身仍断言：

```text
999ms reference 可以显示
```

这种和新的 450ms hard age 明确冲突的行为：

应更新测试。

但必须说明：

```text
为什么修改测试预期
```

不能为了让测试绿而修改实现。

---

# 48. Git 仅作为本地审查工具

允许：

```bash
git status
git diff
git diff --stat
git diff --check
```

用于：

```text
查看修改
发现误改
检查 whitespace
```

必须执行：

```bash
git diff --check
```

但不要：

```text
commit
push
pull
fetch
rebase
merge remote branch
```

除非用户另外明确要求。

---

# 49. 保护本地现有修改

在修改前查看：

```bash
git status
git diff
```

如果存在用户原有未提交修改：

必须保留。

不要：

```text
覆盖
回滚
格式化整个文件导致巨大无关 diff
```

尽量采用：

```text
minimal diff
```

如果修改位置和原有未提交代码重叠：

在当前代码基础上继续修改。

---

# 50. 性能要求

修改不能明显增加：

```text
receiver CPU
内存
compose cost
SR task 数量
网络带宽
```

尤其：

same-source-frame immutable

应该：

```text
缓存 composed frame
```

而不是每个 display tick：

```text
重新 deep-copy + compose 整帧
```

---

# 51. Thread safety

检查：

```text
network receiver
H265 decoder
display thread
SR worker
reference assembler
```

共享的数据：

```text
reference map
generation
SR cache
pending
composed frame
state snapshot
```

推荐：

```text
immutable snapshot
atomic swap
small critical section
```

避免：

```text
render 到一半
SR thread 修改同一个 reference object
```

---

# 52. 最终 render selection

最终逻辑应接近：

```text
NEW SOURCE FRAME
       |
       v
select matching STATE
       |
       v
state generation valid?
       |
      no
       |
      BASE
       |
      yes
       v
select newest matching local-ready reference
       |
       v
reference exists?
       |
      no
       |
      BASE
       |
      yes
       v
generation/refgen valid?
       |
      no
       |
      BASE
       |
      yes
       v
future <=100ms?
       |
      no
       |
      BASE
       |
      yes
       v
content age <=450ms?
       |
      no
       |
BASE + AGEDROP
       |
      yes
       v
geometry/content valid?
       |
      no
       |
      BASE
       |
      yes
       v
valid SR cache?
   /         \
 yes         no
 |            |
ROI-ESRGAN ROI-LANCZOS
      \      /
       v    v
   COMPOSE ONCE
       |
       v
cache by source_sequence
       |
       v
all display ticks for this
source sequence reuse exactly
the same composed frame
```

---

# 53. Reference 生命周期

```text
PATCH PARTIAL
     |
     v
ASSEMBLING
     |
CRC/FEC/JPEG/MASK
     |
     v
LOCAL_READY
     |
     +----------------+
     |                |
age <=450ms       age >450ms
     |                |
DISPLAYABLE          STALE
                      |
               cache TTL expiry
                      |
                      v
                    DELETE
```

注意：

```text
LOCAL_READY
```

不等于无条件 display。

还必须通过：

```text
STATE
generation
future gate
age
geometry
content match
```

---

# 54. SR state machine

```text
NEW REFERENCE
      |
      v
 CANDIDATE
      |
  +---+----+
  |        |
worker   worker
idle     busy
  |        |
check    latest
age      pending
  |        |
valid    newer arrives
  |        |
RUNNING  replace pending
  |
DONE
  |
check:
age
generation
refgen
  |
 +-------+
 |       |
valid   stale
 |       |
CACHE   STALE++
```

---

# 55. Sender 不能破坏 H265 主码流

调整 reference refresh 时必须分析：

```text
H265
STATE
PATCH
FEC
```

带宽竞争。

不要采用：

```text
PATCH unlimited highest priority
```

造成：

```text
H265 queue 增长
端到端延迟增加
decoder starvation
```

优化目标：

```text
reference low latency
```

而不是：

```text
reference maximum throughput
```

---

# 56. 建议配置化

关键配置纳入现有配置系统。

例如：

```text
--rebuild-reference-cache-ttl-ms

--rebuild-reference-hard-age-ms

--rebuild-reference-future-ms

--rebuild-reference-fade-start-ms

--rebuild-reference-soft-refresh-ms

--rebuild-reference-refresh-guard-ms

--rebuild-debug-timing
```

实际名称遵守项目已有 CLI 风格。

默认至少实现：

```text
cache ttl = 1000ms

content hard age = 450ms

future max = 100ms
```

禁止相同常量散落在多个文件。

---

# 57. 文档同步更新

根据最终实际实现更新本地：

```text
启动.md
协议.md
fix.md
README.md
```

只修改相关文档。

文档必须正确解释：

```text
CACHE TTL != CONTENT AGE

PATCH complete => local-ready

450ms hard deadline

100ms future gate

SR latest-only

same-source-frame immutable

async result next-frame-only

reference deadline-driven refresh
```

不能出现：

```text
代码已经 local-ready
但文档仍说必须等下一条 STATE ready
```

---

# 58. 不要求修改线上仓库

本任务结束时：

不需要：

```text
push GitHub
创建 PR
提交 commit
更新 remote
```

最终成果就是：

> 当前本地工作区中的代码修改、测试和文档修改。

用户之后自行决定是否：

```text
commit
push
创建 PR
```

---

# 59. 真机验证

如果当前本地环境连接了 RK3588：

执行实际：

```text
RK3588 sender
↓
真实限速链路
↓
PC receiver
↓
Real-ESRGAN
```

测试：

```text
目标静止
缓慢移动
快速移动
快速转头
bbox 大小变化
遮挡
重新出现
多目标
```

如果当前没有板卡：

不要因此停止整个任务。

先完成：

```text
代码
单元测试
UDP loopback
模拟 jitter
Real-ESRGAN 本机测试（若可用）
文档
```

最后标注：

```text
真实 RK3588 限速链路尚未验证
```

---

# 60. 真机重点指标

建议记录每个 source frame：

```text
source_sequence
video_pts
state_pts
reference_pts
reference_age
generation
reference_generation
render_mode
bbox
match_score
SR state
```

重点寻找：

错误模式：

```text
ROI
BASE
ROI
BASE
ROI
BASE
```

理想：

```text
ROI
ROI
ROI
ROI
```

当 reference 确实过期时允许：

```text
ROI
ROI
BASE
BASE
ROI
```

但不能使用已经超过 450ms 的 reference 来掩盖 BASE gap。

---

# 61. same-source-frame 真机验收

同一个：

```text
source_sequence=N
```

如果 display 输出两次甚至更多次：

这些输出必须：

```text
pixel-identical
```

SR/PATCH/STATE 在中间到达：

不得改变该 source frame。

---

# 62. 最终成功标准

## Reference

```text
PATCH 完整后立即 local-ready

>450ms reference 永不显示

>100ms future reference 永不显示

old generation 永不覆盖 new generation
```

## SR

```text
1 running

<=1 pending

latest pending only

expired candidate 不执行

stale result 不显示
```

## Display

```text
同一个 source frame compose exactly once

same source frame pixel immutable
```

## Sender

```text
reference refresh 不再天然明显长于 450ms hard deadline
```

同时不能破坏：

```text
总链路限速
H265 稳定性
```

## Fallback

没有合法 reference：

```text
稳定 BASE
```

而不是错误旧 ROI。

---

# 63. 不接受的伪修复

以下任何一种不能认为任务完成：

```text
只把 1.0 改成 0.45

只提高 PTS tolerance

只延长 STATE cache

只延长 old reference 生命周期

只关闭 ESRGAN

只关闭 ROI

只增加 bbox smoothing

只修改 HUD

只改测试让测试通过

只做 UDP loopback

只读 goal 不改代码

只分析线上 GitHub、不检查本地代码
```

---

# 64. 修改优先级

严格优先：

```text
P0
same-source-frame immutable

P0
PATCH local-ready

P0
cache TTL / content age separation

P0
generation fail-closed

P0
SR submit/final age gate

P1
latest-only SR scheduler

P1
sender reference deadline refresh

P1
reference delivery telemetry

P1
HUD AGEDROP/Q/X/STALE

P2
fade-out

P2
动态 JPEG/reference bandwidth 优化
```

---

# 65. 推荐实际执行顺序

```text
1. git status / git diff
   了解本地未提交修改

2. 阅读本地核心代码

3. 对照本 goal 和 fix.md
   判断已实现/部分实现/未实现

4. 定位当前 reference 生命周期

5. 实现 PATCH local-ready

6. 分离 cache TTL / content hard age

7. 强化 generation/refgen fail-closed

8. 修改 SR scheduler 和双重时效 gate

9. 实现 source_sequence compose-once cache

10. 保证 async result next-frame-only

11. 分析 sender reference refresh

12. 实现 bandwidth-aware deadline refresh

13. 增加 telemetry/HUD

14. 增加和更新 tests

15. 跑完整测试

16. 跑 UDP loopback/jitter 测试

17. 有 CUDA 时跑真实 Real-ESRGAN

18. 有 RK3588 时跑真实链路

19. 更新本地文档

20. git diff --check

21. git diff 最终人工式审查

22. 删除临时 debug、重复实现和无关修改

23. 输出完整结果报告
```

---

# 66. Agent 最终必须给出的结果

完成后输出：

```text
# Result

## 1. Local Workspace Status

开始修改前本地是否存在未提交修改，
如何保证没有覆盖。

## 2. Root Causes

根据本地代码实际确认的抖动原因。

## 3. Existing Fixes

哪些机制本地原本已经存在，
因此本次没有重复实现。

## 4. Implemented Changes

按文件说明实际修改。

## 5. Timing Model

最终：

cache TTL
content hard age
future max
state sync
refresh deadline

分别是多少、分别负责什么。

## 6. Reference Lifecycle

local-ready 和 reference 状态机。

## 7. SR Scheduler

running/pending/cache/stale 的最终行为。

## 8. Source Frame Consistency

如何保证：

same-source-frame immutable
async-result-next-frame-only

## 9. Sender Refresh

如何避免 refresh interval 与 450ms 冲突，
以及如何避免 reference 挤占 H265。

## 10. Tests

实际执行的命令。

每个测试实际：
PASS / FAIL

不能只写“测试正常”。

## 11. UDP/Jitter Verification

实际结果。

## 12. Real-ESRGAN Verification

真实 CUDA 验证或未验证。

## 13. RK3588 Verification

实际验证或明确未验证。

## 14. Modified Files

列出所有修改文件。

## 15. Remaining Risks

尚存风险和建议。

## 16. Recommended Run Command

最终本地推荐运行命令。

## 17. Git Diff Status

git diff --check 结果。

明确说明：
未执行 git push/pull/fetch，
未修改线上仓库。
```

---

# 67. 最终六条架构原则

整个修改以以下原则为最高约束：

```text
1. NEW REFERENCE EARLY

PATCH 完整后新 reference 尽早本地可用。


2. OLD REFERENCE HARD EXPIRE

旧 reference 严格按照内容 PTS 到期。


3. FAIL CLOSED

无法证明 reference 与当前 source frame 匹配，
立即回 BASE。


4. SOURCE FRAME IMMUTABLE

同一 source video frame 一旦 compose，
整个显示期间像素不得改变。


5. ASYNC RESULT NEXT FRAME ONLY

PATCH、STATE、SR 等异步结果，
只能影响后续 source frame。


6. BANDWIDTH-AWARE REFRESH

reference 更新必须适应实际低带宽链路，
尽量在 450ms deadline 前完成下一代，
同时不得挤坏 H265 主码流。
```

最终目标：

> 在当前本地 VideoCompress 工程中，完成可测试、可验证、可维护的 temporal-consistency 重构，使 RK3588 → PC 低带宽视频重建链路中的 ROI 在 PTS、generation、reference 生命周期、几何位置以及 SR 状态上保持一致，消除旧 reference、STATE ready 延迟、SR 异步完成和同源帧重复 compose 引起的肉眼抖动和闪烁。

**整个任务只针对当前本地工作区执行，不需要、也不要直接修改线上 GitHub 仓库。**