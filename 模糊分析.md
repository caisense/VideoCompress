我重新按 **401c9bc** 的代码和你这张 HUD 对照后，判断和前几轮又有了变化：

**这张截图中的“长时间人脸模糊”，主要已经不是 STATE/AGE/SR 没命中的问题，而是当前 reference 本身给人脸留下的有效信息太少。**

最关键的证据是：截图这一刻明明处于

```text
FRAME HOLD+ROI-ESRGAN
REFREADY 1
USED 1/1
PTSAGE +163ms
DROP_REASON NONE

SR Real-ESRGAN/CUDA
RUN 0
Q 0
```

也就是说此刻：

- reference 存在；
- reference 很新；
- ROI 确实贴上去了；
- Real-ESRGAN 确实命中了；
- GPU 没排队；
- 当前没有 STATE / AGE / CONTENT drop；

**但人脸仍然明显模糊。**

这说明应该把注意力从“为什么没用 ESRGAN”转向：

> **ESRGAN 拿到的究竟是什么质量的输入？**

------

# 1. 401c9bc 已经修掉了很多旧问题

这次提交并不是没按前面的方案做。它已经加入了 170ms 的 rebuild playout buffer，等待 STATE/video 会合后再不可变合成；还加入了 bounded state extrapolation。

另外：

- registration 已经改成固定 raw-JPEG template，不再拿 ESRGAN 输出做匹配；
- bbox EMA 已不再跟 `reference_generation` 一起重置；
- 新 reference SR 没 ready 时，可以短暂 handover 到上一代仍然有效的 ESRGAN reference；
- handover 同样受 450ms hard age 约束。

所以以前的：

```text
STATE → BASE
AGE500 → BASE
ESRGAN → Lanczos → ESRGAN
```

这些问题在 401c9bc 已经得到明显针对性修复。

但是这次修改主要解决的是：

> **“ROI 有没有稳定地用上”**

并没有解决：

> **“ROI 本身有没有足够的人脸信息”。**

这是现在最重要的区别。

------

# 2. 当前最大瓶颈：一整个人被压缩到最长边最多 96 像素

发送端现在不是单独裁人脸。

它拿的是 YOLOv8-Seg 的**整个 person bbox**，而且 bbox 四周还增加：

```text
crop_margin_percent = 20%
```

然后才把整个 crop 缩小到：

```text
patch_max_side = 96
```

。

代码非常明确：

```cpp
left   = bbox.left   - margin_x;
top    = bbox.top    - margin_y;
right  = bbox.right  + margin_x;
bottom = bbox.bottom + margin_y;

scale =
    min(1.0,
        96 / max(crop_width, crop_height));

resize(...)
```



这对“整个人”而言问题很大。

假设一个站立人物：

```text
+--------------------+
|     20% margin     |
|                    |
|       O  ←脸       |
|      /|\           |
|      / \           |
|                    |
|     20% margin     |
+--------------------+

整张图最长边
↓
压到 96 pixel
```

人的实际身体因为上下各20% margin，可能只占这96像素中的六七十像素左右。

然后**脸又只占身体高度的一小部分**。

因此送到PC的 reference 中，人脸很可能实际上只有类似：

```text
10×10
15×15
20×20
```

量级的有效信息。

具体多少取决于人物距离和 bbox，不能只凭截图给出精确值，但机制就是这样。

------

# 3. 更严重的是，这个 reference 还只有最多 800 Bytes

当前默认：

```text
JPEG quality = 50
JPEG max bytes = 800
```

。

如果第一次 JPEG 超过 800B：

```cpp
Q50
 ↓
Q43
 ↓
Q40
```

还大：

```cpp
scale *= 0.82
```

然后继续重新编码。

所以实际上某一代 person reference 可能从：

```text
96×50 Q50
```

退化成：

```text
96×50 Q43
```

甚至：

```text
79×41 Q40
```

再极端一点继续缩。

而且 JPEG 压的是：

> **person + 周围20%背景 margin**

不是只压 segmentation foreground。

这意味着宝贵的800B还被：

```text
衣服
身体
背景
桌子
边缘
```

共同消耗。

真正留给：

```text
眼睛
鼻子
嘴巴
眉毛
```

的信息量非常低。

------

# 4. 这正好解释为什么 HUD 显示 ROI-ESRGAN，但脸还是糊

当前 ESRGAN 输入尺寸默认：

```text
96×96
```

。

但代码不是把一个小 reference 强行变成96×96的真实细节。

它做的是保持比例：

```python
fit = min(
    1.0,
    96 / max(source_width, source_height)
)
```

如果 reference 本来就小于96：

**甚至不会先把内容放大到96**，只是居中 padding。

随后 Real-ESRGAN x2，再把结果 Lanczos resize 到最终人物大小。

例如概念上：

```text
发送来的 person JPEG
96×45

其中脸可能只有
约 15×15

        ↓ Real-ESRGAN x2

person 有效内容
192×90

脸有效内容
约 30×30

        ↓

最终人物要贴回
640×360输出中的大ROI

        ↓ Lanczos再次拉伸

人脸可能显示成
几十像素甚至更大
```

所以最终：

```text
FRAME ROI-ESRGAN
```

并不等于：

```text
人脸拥有高清真实细节
```

它只能说明：

> **这张极低信息量 person reference 已经经过了 ESRGAN。**

ESRGAN 无法恢复发送端已经彻底丢掉的真实眼睛、嘴巴等信息。

------

# 5. 这张截图本身就是很强的反证

如果现在主要问题还是 SR 调度，我们应该看到类似：

```text
ROI-LANCZOS
SR MISS
RUN 1
Q 1
```

但你截图：

```text
ROI-ESRGAN

RUN 0
Q 0

DONE 1250/1308
STALE 58

LAT 44/68/124ms
```

说明 CUDA 运算能力完全不是当前瓶颈。

`p50≈68ms`、`p95≈124ms` 对 6fps：

```text
166.7ms / source frame
```

已经是可以接受的。

而且：

```text
SRWAIT FUT 0
```

说明我们之前讨论的 future reference 阻止预计算的问题现在也基本消失了。

------

# 6. `AGEDROP 107 / STATEDROP 184` 为什么这么大？

这两个值仍然需要继续关注：

```text
AGEDROP 107
STATEDROP 184
```

它们说明**整个运行历史上仍然发生过不少 fallback**。

所以如果你看到：

```text
突然整个目标变得特别模糊
然后又恢复
```

它们仍可能是原因。

但注意这是**累计 counter**。

你截图这一刻：

```text
DROP_REASON NONE
REFREADY 1
USED 1/1
ROI-ESRGAN
PTSAGE +163ms
```

因此它们不能解释：

> **“为什么当前这一帧已经 ROI-ESRGAN 了，人脸仍然像马赛克一样糊。”**

当前这个问题已经是**质量上限**而不是 eligibility 问题。

------

# 7. 还有一个很容易误读的地方：`SRC 256×144` 不是 reference 原始来源分辨率

这一点需要特别修正。

HUD：

```text
SRC 256×144 @6fps
```

指的是当前 **H.265 视频 profile**。

401c9bc 中摄像头先保留完整 RGB：

```cpp
converted.source_width  = rgb.cols;
converted.source_height = rgb.rows;
converted.rgb = ...
```

然后另外通过 RGA 缩成 encoder NV12。

而 `RebuildSender` 裁 reference 用的是：

```cpp
request.frame->source_width
request.frame->source_height
request.frame->rgb
```

也就是说：

```text
Camera full RGB
       │
       ├──────────────→ person reference crop
       │
       └→ RGA → 256×144 → H265
```

。

所以不是：

```text
256×144
↓
裁人
↓
ESRGAN
```

问题发生在更后面：

```text
高分辨率RGB
↓
裁整个person + 20% margin
↓
强制最长边 ≤96
↓
JPEG ≤800B
```

这里才是真正把人脸细节压掉了。

------

# 8. 为什么有时会“长时间”模糊，而不是只糊一帧？

这里有三个放大因素。

第一，你的视频实际只有：

```text
SRC 6fps
```

但显示：

```text
OUT 12fps
```

HUD：

```text
TEMP HOLD 49.9% (6→12)
```

说明大约一半显示帧只是重复上一张 source composition。

因此一张比较糊的 source frame：

```text
166ms
```

实际上会在12fps窗口连续展示两次。

这使模糊的主观持续时间更明显。

第二，401c9bc 默认又增加了：

```text
playout delay = 170ms
```

它本身不会降低画质，但会让你看到的是较早的画面。

截图：

```text
Source age 398ms
```

也不能简单理解为“解码器卡了398ms”，因为代码把 `current_updated` 记录成**这张 source frame 最早到达PC的时间**，而不是最终 compose/display 时间；170ms playout 已经被包含在 Source age 里。

第三，新代码还有 ESRGAN handover。

如果新 reference SR 尚未完成，可以继续使用上一代：

```text
旧 ESRGAN reference
```

只要它仍然：

```text
AGE <=450ms
```

。

这避免了：

```text
清晰→Lanczos→清晰
```

但如果上一代 reference 本身就是一张低细节人脸，它也会让这张“已经SR但本质模糊”的人脸持续几百毫秒。

所以它解决的是：

> **锐度跳变**

但不能解决：

> **源 reference 信息不足。**

------

# 9. 我认为当前最大的架构问题：你在做“人物增强”，但视觉目标其实是“人脸清晰”

当前语义层级：

```text
YOLOv8-Seg
↓
person
↓
整个人一张 reference
↓
96px / 800B
↓
ESRGAN
```

但你真正观察质量的地方是：

```text
FACE
```

这是两个不同的优化目标。

如果人物占画面较大：

```text
身体
衣服
胳膊
腿
背景margin
```

都在争抢96像素和800B。

因此对于人脸：

> **码率分配策略实际上是不合理的。**

------

# 10. 我现在最推荐的方案不是继续调 ESRGAN，而是增加“Face sub-ROI”

架构改成：

```text
               ┌→ Person reference
Camera → YOLO ─┤   低/中分辨率
               │   用于身体、轮廓
               │
               └→ Face reference
                   高空间密度
                   ↓
                  PC
                   ↓
                Face SR
```

接收端：

```text
低码率 H265 base
       ↓
Person ROI reconstruction
       ↓
Face ROI reconstruction
       ↓
最终画面
```

这样相同几百字节：

### 现在

```text
800B
↓
整个人
↓
脸只占其中很小一部分
```

### Face ROI

```text
400~800B
↓
只编码脸
↓
眼鼻嘴拥有几十倍更高的像素密度
```

这对视觉质量的收益会远大于：

```text
Real-ESRGAN x2
→ x4
```

或者继续调 SR latency。

------

# 11. 但不能直接把 96/800 提高一倍

截图：

```text
H265 17.8 kbps
RB   63.0 kbps

V+RB WIRE 94.5 kbps
LINK CAP 100 kbps incl audio
```

说明现在已经非常接近100kbps硬上限。

只剩很少余量。

如果直接：

```text
96 →192
800B →2000B
```

很可能结果变成：

```text
PATCH抢码率
↓
H265排队
↓
P帧延迟
↓
视频断续
↓
reference也更晚
```

所以不能简单“加码率”。

------

# 12. 更合理的是重新分配现有 RB 63kbps

我会优先考虑：

```text
当前：
整个人 reference
≈3次/s
每次最高800B
+ mask/state/FEC

改为：

Person context
低频率 / 更低质量
        +
Face reference
较高空间密度
```

例如概念上：

```text
Person：
只负责轮廓/颜色/衣服

Face：
负责眼鼻嘴
```

不一定增加总 RB 码率，只是：

> **把现有 bit 从不重要的身体/背景 margin 搬到人脸。**

这实际上比换 CompressAI 都更应该优先做。

------

# 13. 在改架构之前，我强烈建议先增加4个 HUD 指标

目前 HUD 最大的问题是：

你看到：

```text
ROI-ESRGAN
```

却看不到 ESRGAN 到底增强了一张什么分辨率的 JPEG。

发送端应该把以下信息加入 PATCH metadata / telemetry：

```text
JREF 96x43
JPEG 742B
Q 43

FACEPX 14x16   // 如果有face detector
```

接收端再显示：

```text
REF 96x43 / 742B / Q43

SR NATIVE 192x86
PASTE 178x312

UPSCALE 3.6x
```

这个指标非常重要。

现在你只知道：

```text
ESRGAN HIT
```

不知道：

> ESRGAN 是把一张 `96×50` 的好 reference 增强了，还是把一张因为800B限制被缩成 `52×28` 的 reference 增强了。

这两者视觉效果完全不是一回事。

------

# 14. 我建议你做一个非常简单的 A/B 实验

暂时只用于定位，不作为正式配置：

把 sender 收到的 reference JPEG 保存出来，例如：

```text
debug_ref/
track1_gen305.jpg
track1_gen306.jpg
...
```

PC同时保存：

```text
raw/
gen305.jpg

sr_native/
gen305.png

final_roi/
gen305.png
```

然后直接看：

### 情况A

如果：

```text
raw JPEG中的脸已经只有十几个像素
```

那就可以彻底确认：

> **瓶颈在发送端 reference 设计。**

继续优化 ESRGAN 时序没有意义。

### 情况B

如果 raw reference 中脸非常清楚，而：

```text
SR native
```

反而变糊，

才应该继续研究：

```text
RealESRGAN_x2_dynamic模型
输入预处理
训练域
模型本身
```

我根据当前代码，认为出现 **A 的概率明显更高**。

------

## 15. ESRGAN 本身也不是专门的人脸恢复器

你当前模型是：

```text
RealESRGAN_x2_dynamic.onnx
```

它做的是通用图像恢复。

Real-ESRGAN 官方项目本身也额外集成了 **GFPGAN** 来做专门的 face enhancement，这恰恰说明“通用 Real-ESRGAN”和“人脸恢复”不是同一件事。([GitHub](https://github.com/xinntao/real-esrgan?utm_source=chatgpt.com))

但是我不建议现在马上：

```text
ESRGAN → GFPGAN
```

因为即使 GFPGAN 能让脸“看起来更像脸”，如果原始 reference 只有十几个像素，它生成的很多东西也是模型推测的。

如果这个项目重视：

```text
真实性
身份特征
监控取证
```

应该优先：

> **发送更多真实 face information**

而不是让生成模型猜细节。

------

# 我的当前根因排序

结合 **401c9bc + 这张截图**，我现在会这样排序：

| 优先级 | 原因                                                         | 判断                               |
| ------ | ------------------------------------------------------------ | ---------------------------------- |
| **P0** | 整个人+20% margin被压到最长边96                              | **最主要**                         |
| **P0** | 整张人物JPEG只有≤800B，复杂时还继续降Q/缩图                  | **最主要**                         |
| **P0** | 没有 Face sub-ROI，bit没有优先给脸                           | **架构核心问题**                   |
| P1     | Real-ESRGAN只是x2通用SR，随后还要Lanczos放大到大ROI          | 明显贡献                           |
| P1     | 6fps→12fps HOLD使糊帧持续更明显                              | 放大现象                           |
| P1     | SR handover可能维持一张较软旧reference数百ms                 | 放大持续时间                       |
| P2     | STATE/AGE drop                                               | 仍存在，但**不是截图这一刻的原因** |
| P2     | CUDA速度                                                     | 从HUD看基本不是瓶颈                |
| 已修   | future SR、EMA refgen reset、SR/template registration coupling | 401c9bc已有针对性修复              |

一句话概括：

> **401c9bc 现在已经能够比较稳定地把“人物ROI”贴出来了，但它传输的是一张“最长边仅96、最多800B、还包含20%背景边距的整个人图片”。对人脸而言，输入信息在到达 ESRGAN 之前就已经严重不足，因此即使 HUD 显示 `ROI-ESRGAN`，也完全可能连续很长时间保持模糊。**

下一轮代码修改方向我建议直接从“解决时序 bug”切换到 **“在不突破100kbps的前提下进行 face-aware bit allocation”**。