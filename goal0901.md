/goal

# VideoCompress rebuild 人像模糊/清晰跳变最终时序一致性修复

# 0. 工作范围

只修改当前本地工作区。

禁止：

git pull
git fetch
git push
git reset --hard
git checkout -- .
git restore .
git clean -fd

开始前执行：

git status
git diff

保护用户所有已有未提交修改。

当前线上仓库最新提交 27e60e1 仅更新 debug.txt，
实际代码基线仍为上一版 479cd56 对应实现。

本任务必须以当前本地源码为最终事实来源。

读取本 goal 后不要停止在：

“已读取目标文件”

必须继续：

检查代码
→ 解析 debug
→ 重现 bug
→ 写回归测试
→ 修改实现
→ 执行测试
→ 条件允许时真机验证
→ 输出前后统计

---

# 1. 当前问题重新定性

不要再把以下问题作为当前主根因：

future PTS 符号写反

该问题当前逻辑已经正确。

也不要把：

ROI-LANCZOS ↔ ROI-ESRGAN

作为唯一或首要根因。

最新 debug 表明当前更严重的是：

ROI-ESRGAN
      ↓
BASE-LANCZOS
      ↓
ROI-ESRGAN

即 ROI 整体频繁失效。

本轮优先解决：

P0：STATE 与 video 的时序竞争

P0：CONTENT registration 大量误拒绝

P0：reference AGE 仍偶发达到 500ms

P1：SR/Lanczos quality handover

P1：bbox EMA 每 refgen 重置

P1：track/reference generation 连续性

---

# 2. 当前已经确认正确的机制不要推翻

以下机制继续保留：

reference cache TTL = 1000ms

reference content hard age = 450ms

future render gate = 100ms

PATCH complete 后 local-ready

future reference 可以提前 SR compute

future reference 不允许提前 render

SR：
1 running + <=1 latest pending

old generation 不覆盖 new generation

same-source-frame immutable

source frame compose once

capture-time based reference refresh scheduler

目标正常平移不再被25% center gate直接拒绝

不要重新修改 PTS 符号。

不要通过：

450ms → 700/1000ms

掩盖问题。

---

# 3. 必须首先自动解析最新 debug.txt

不要只肉眼阅读。

写一个临时分析脚本，例如：

tools/analyze_rebuild_debug.py

或者任务完成后可删除的临时脚本。

必须统计：

总 source frame 数

MODE：
BASE-LANCZOS
ROI-LANCZOS
ROI-ESRGAN

DROP：
STATE
CONTENT
AGE
NOREF
GEN
GEOM_SCALE
GEOM_INVALID
GEOM_OUTSIDE
FUTURE
NONE

并统计 transition：

ESRGAN → BASE
BASE → ESRGAN

ESRGAN → LANCZOS
LANCZOS → ESRGAN

连续 BASE 最大长度

连续 STATE DROP 最大长度

连续 CONTENT DROP 最大长度

reference AGE：
p50
p95
max

STATE sync：
p50
p95
max absolute delta

new RGEN first-frame：
ESRGAN
LANCZOS
BASE

输出分析结果作为修改前 baseline。

---

# 4. 最新 debug 已确认：STATE 是当前首要问题之一

日志存在大量：

VPTS = current frame

SPTS ≈ VPTS - 166/167ms

然后：

DROP=STATE
MODE=BASE-LANCZOS

166.7ms 正好等于：

1000 / 6 fps

即一张 source frame。

甚至出现连续多帧：

SEQ73
SEQ74
...
SEQ84

持续 STATE fallback。

因此当前视觉上出现的明显模糊，
大量不是 ESRGAN 算力问题，

而是：

没有合法 STATE
→ references 不进入 composer
→ 整个人像直接退回 BASE。

---

# 5. 当前 scene_synced() 存在逻辑矛盾

当前代码流程大致：

选择最近 STATE

↓

允许 _extrapolate_state() 做有限运动预测

调用时外推窗口实际可达到：

max(max_sync_ms * 4, 400)

即约400ms

↓

但随后：

if abs(selected_delta) > max_sync_ms:

默认 max_sync_ms=100

直接：

return None

这意味着：

已经能够安全外推的
166ms 一帧滞后 STATE

最终仍然被完全丢弃。

必须重新设计。

不要简单：

100ms → 200ms

然后结束。

---

# 6. P0：引入 rebuild playout buffer

当前代码：

新的 H265 source frame 解码完成

↓

live_h265_hud.py 立即：

scene_synced(source_pts)

↓

立即 compose

这会产生 race：

H265 frame N 先到

↓

对应 STATE N
可能晚几毫秒或几十毫秒到

↓

frame N 已经被 compose 为 BASE

↓

same-source-frame immutable

↓

STATE N 到达后也不能修复 frame N

这是当前架构的重要问题。

---

# 7. 推荐实现：短时 presentation/rebuild buffer

增加：

--rebuild-playout-delay-ms

不要直接固定死。

建议初始默认：

170ms

约一张 6fps source frame。

但 agent 必须首先根据实际 STATE arrival telemetry 判断：

50ms
100ms
170ms

哪个足够。

设计：

decoded frame N
+
source_sequence
+
RTP PTS
+
arrival monotonic time

进入一个有界队列。

只有：

now - frame_arrival >= playout_delay

才允许：

scene_synced()
+
composer.render()

---

# 8. playout buffer 必须严格有界

不要形成越来越大的视频延迟。

例如：

deque max 3~4 source frames

每次只 compose：

最早已经满足 delay 的 frame

如果消费落后：

丢弃过旧未显示 frame

而不是无限积压。

要求端到端额外延迟：

可测量
可配置
可关闭。

--rebuild-playout-delay-ms=0

必须能够恢复当前行为，
方便 A/B 测试。

---

# 9. playout delay 的目标不是降低 fps

source 仍为：

6fps

output 仍为：

12fps HOLD

逻辑变为：

frame N decoded
↓
buffer 约170ms
↓
compose exactly once
↓
12fps display HOLD

same-source-frame immutable 继续成立。

不能因为增加 buffer：

同一个 source frame compose 两次。

---

# 10. 增加 STATE arrival telemetry

当前 debug 只有：

VPTS
SPTS

但不知道：

对应 STATE 究竟是：

没发送
丢包
晚到
还是 compose 太早。

必须给 STATE history 保存：

packet_pts
arrival_monotonic_time
frame_id
sequence

debug 增加：

STATE_DELTA
STATE_ARRIVAL_AGE
STATE_SELECTED_AGE
STATE_EXTRAP
STATE_REASON

例如：

STATE_DELTA=-167
STATE_ARRIVAL_AGE=18ms
STATE_EXTRAP=1
DROP=NONE

或者：

STATE_DELTA=-334
STATE_ARRIVAL_AGE=410ms
DROP=STATE_TOO_OLD

---

# 11. 把 STATE DROP 原因拆分

目前统一：

DROP=STATE

信息不足。

至少拆成：

STATE_NO_HISTORY

STATE_TOO_OLD

STATE_TOO_FUTURE

STATE_GEN_MISMATCH

STATE_TARGET_EMPTY

STATE_TARGET_MISSING

STATE_ARRIVED_LATE

如果能判定。

不要把所有失败都记成 STATE。

---

# 12. 修复“外推后仍按原始 delta 拒绝”的矛盾

如果保留 state extrapolation：

必须明确两个概念：

raw_state_delta

和：

effective_state_delta

例如：

raw STATE：
-167ms

经过连续 track + velocity extrapolation：

有效几何预测到当前 PTS

此时不能继续仅根据原：

-167ms

把整个结果 reject。

但也不能无条件接受任意旧 STATE。

---

# 13. 推荐安全规则

优先级：

A. 有 <=100ms 的真实 STATE

直接使用。

B. 没有，但有一帧以内约 <=200ms 的历史 STATE

且满足：

同 generation
同 track_id
class不变
上一条STATE也存在
速度可估计
没有 track switch
没有明显bbox异常

允许有限 extrapolation。

标记：

STATE_EXTRAP=1

C. 超过安全窗口

fail closed → BASE。

不要把最大窗口直接扩到400ms后无条件显示。

---

# 14. playout buffer 优先于无限扩大 STATE gate

优先通过：

让数据到齐后再 compose

解决时序 race。

state extrapolation 只是：

少量丢包/抖动时的 fallback。

不能用：

STATE_SYNC_MAX_MS=400

替代 proper synchronization。

---

# 15. 检查 sender STATE 是否因为 RatePacer 晚于 H265

当前 sender：

sendState(request)

同样调用：

pacer_->waitForTokens()

STATE 虽然在 rebuild process 中先发送，

但视频和 rebuild 共享 100kbps pacer。

必须统计：

source frame capture time

STATE send start

STATE actual send time

PATCH send time

H265 RTP send time（如果容易取得）

以确认：

对应 STATE 比 H265 frame 晚多少。

不要猜。

---

# 16. 如果 STATE 经常因 pacer 排队明显晚到

优先方案：

playout buffer。

其次才研究：

control packet scheduling priority。

如果要调整 priority：

仍不得突破100kbps物理总预算。

禁止：

STATE绕过RatePacer无限发送。

可以研究：

给 STATE 保留极小 control budget

或者：

在 source frame packet scheduling 中提前 STATE

但必须证明有必要。

---

# 17. 第二个 P0：CONTENT DROP

最新 debug 有大量：

AGE=160~335ms

REFREADY=1

DXR≈0.00~0.05

DYR≈0

AREA_RATIO≈0.97~1.05

甚至：

SR cache HIT

但：

DROP=CONTENT
MODE=BASE-LANCZOS

说明：

reference 时效正确

generation正确

geometry正常

SR已经完成

仍然因为 content matcher 失败而把整个人像打回 BASE。

这是当前第二大问题。

---

# 18. 当前 _content_register() 使用“渲染图”做模板

当前流程：

_assets()

↓

如果 SR cache hit：
patch = ESRGAN output

否则：
patch = raw JPEG / Lanczos

↓

_content_register(
    canvas,
    patch,
    mask
)

也就是说 registration template 会随画质路径变化：

Lanczos template

和：

ESRGAN template

不是同一个图像域。

而 canvas 是：

极低码率 H265 base
再上采样后的图像。

Real-ESRGAN 会改变：

高频纹理
边缘
局部对比
人脸细节

因此：

SR图
vs
低码率H265图

直接做 TM_CCOEFF_NORMED

稳定性很差。

---

# 19. P0：registration template 与 render asset 解耦

必须改成：

registration

永远使用稳定的 reference template

而不是：

当前准备贴上去的 ESRGAN/Lanczos patch。

推荐：

VisualReference 增加或按需生成：

registration_luma

来源：

原始 JPEG reference.image

↓

固定 grayscale

↓

固定 resize / low-pass

↓

必要时只取 segmentation foreground

形成：

稳定 registration template。

---

# 20. 正确流程应变成

geometry prediction

↓

使用 raw-reference registration template
与 current base 做 registration

↓

得到 corrected x/y

↓

最后才选择：

ESRGAN asset
或
Lanczos asset

↓

blend

即：

registration 与 SR 是否 ready
完全解耦。

这样：

LANCZOS → ESRGAN

不会改变匹配位置和匹配分数。

---

# 21. 不要直接删除 content safety gate

CONTENT gate 的意义是：

防止把旧人物贴到错误背景。

必须保留 fail-closed。

但需要修：

错误模板域导致的误拒绝。

不要简单：

minimum_score=0

或者：

永远接受 geometry position。

---

# 22. 把 CONTENT 失败原因继续拆分

最新日志大量：

DROP=CONTENT
MATCH=none

这是不够的。

根据当前代码，
MATCH=none + CONTENT 很可能意味着：

patch/mask shape mismatch

或者：

patch尺寸超出canvas

而不一定是：

correlation score低。

必须返回结构化结果。

建立：

ContentRegistrationResult

至少包含：

position
score
attempted
reason

reason：

CONTENT_OK

CONTENT_LOW_SCORE

CONTENT_SHAPE

CONTENT_PATCH_TOO_LARGE

CONTENT_TEMPLATE_INVALID

CONTENT_SEARCH_INVALID

CONTENT_LOW_TEXTURE

CONTENT_CV_ERROR

---

# 23. 特别检查 patch > canvas 的逻辑

当前 _content_register()：

if width > canvas.width
or height > canvas.height:

直接：

return None, None, True

↓

DROP=CONTENT

但 _blend() 本身支持：

对越界区域进行 clipping。

这两部分语义不一致。

如果 reference crop 因 margin 或投影
只是部分超出画面：

不应该因为完整 patch 比 canvas 稍大

直接拒绝整个人物。

需要支持：

clipped registration region。

---

# 24. 实现 clipped registration

如果 predicted crop：

部分超出输出边界

应该：

计算 visible intersection

同步裁剪：

registration template
mask
search region

保持原坐标映射

然后对可见区域做匹配。

只有：

有效可见区域太小

才 fail closed。

不要把部分越界目标整帧打成 BASE。

---

# 25. content registration 必须测试 ESRGAN/Lanczos 一致性

同一个：

reference
target bbox
base frame

分别用：

SR cache miss

和：

SR cache hit

两种情况下，

registration offset 应基本一致。

测试要求：

position delta <= 1~2 px

并且不能：

Lanczos accepted
ESRGAN rejected

仅因为 SR 改变了模板。

---

# 26. 第三个问题：AGE DROP 仍然存在

最新 debug 仍出现：

AGE=500
501
505

→ BASE。

说明：

479cd56 的 refresh scheduler
虽然改善了典型情况，

没有彻底保证：

当前 STATE 指向的 reference

永远 <=450ms。

不要把 hard age 放宽。

---

# 27. AGE 必须结合 STATE lag 一起分析

注意：

AGE DROP 不一定表示：

sender 没有新的 PATCH。

因为 scene_synced() 当前要求：

使用 selected STATE 精确指定的 reference_generation。

可能出现：

本地已经有 RGEN N+1/N+2

但 selected STATE 仍然是旧：

RGEN N

↓

精确查旧 reference

↓

旧 reference AGE=500

↓

AGE DROP。

因此首先统计：

LOCAL_LATEST_RGEN

STATE_RGEN

SELECTED_RGEN

差值。

debug 增加：

LOCAL_RGEN
STATE_RGEN
USED_RGEN
RGEN_LAG

---

# 28. 如果 AGE DROP 是 STATE lag 导致

不要再次调整 sender refresh。

应由：

playout synchronization

解决。

只有证明：

STATE已经是最新RGEN

但 reference仍然500ms

才继续调整：

sender refresh scheduler。

---

# 29. 如果确实仍有 sender refresh 延迟

输出：

capture_age

ready_age

estimated_delivery

refresh_threshold

refresh_reason

pending_reference_active

patch queue

per track

特别注意多目标场景：

一个 pending_reference 全局唯一

可能导致 track1 正在发送时：

track2 不能刷新

从而某个目标 reference age >450ms。

必须验证。

---

# 30. 多目标 reference scheduler

当前只有：

pending_reference_

单个全局传输。

如果 debug 证明多目标互相饿死：

需要实现公平调度。

但不要直接并行发送多个 PATCH。

继续：

同一时间只有一个 physical transfer

但 candidate scheduler 应：

优先选择最接近 hard deadline 的 track。

例如：

EDF
Earliest Deadline First

而不是始终：

highest confidence / first active。

只有 debug 证明多目标 starvation 后才修改。

---

# 31. 第四个问题：bbox EMA 每 reference generation 重置

当前：

_smooth_target()

key：

(
 generation,
 track_id,
 reference_generation
)

这仍然错误。

reference_generation 通常频繁变化。

导致：

每换 reference
→ EMA history 清零。

改成：

(
 generation,
 track_id
)

平滑应该属于：

目标轨迹

不是：

参考图代次。

---

# 32. EMA reset 条件

只有以下情况 reset：

profile generation变化

track_id变化

bbox中心发生明显跳变

bbox尺度极端跳变

长时间失踪后重新出现

不要因为：

reference_generation +1

reset。

---

# 33. active_smooth_keys 同步修改

render() 当前也维护：

active_smooth_keys

其中包含 reference_generation。

同步修改所有：

smooth key
cleanup key
test expectation

避免旧key泄漏。

---

# 34. 第五个问题：SR quality handover 仍需解决，但降为 P1

最新 debug 中仍存在：

ROI-LANCZOS

随后：

ROI-ESRGAN

例如新 reference SR MISS。

所以之前：

SR-ready two-phase handover

仍然有价值。

但最新 debug 说明：

STATE/CONTENT → BASE

比 L/E 画质切换更严重。

必须先解决 BASE fallback。

---

# 35. 完成 P0 后再实现 SR quality hysteresis

当一个 track 已经：

ACTIVE_ESRGAN(ref N)

新 ref N+1 PATCH 到达：

不要立即主动降级：

Lanczos(N+1)

如果旧 ref N：

age <=450ms

geometry仍合法

track仍一致

则可以短暂保持：

ESRGAN(N)

等待：

ESRGAN(N+1)

ready。

然后在 source frame 边界：

原子切换。

---

# 36. handover 安全约束

禁止为了维持 ESRGAN：

使用 >450ms old reference。

如果 old N 过期：

而 N+1 SR 未完成，

允许：

ROI-LANCZOS(N+1)

这是正确 emergency fallback。

优先级：

valid ESRGAN old
>
>valid Lanczos new
>
>BASE

但：

old 必须满足全部 PTS/track/geometry安全条件。

---

# 37. 注意 handover 不能绕过 STATE

不能：

STATE已经明确换成另一 track

还继续贴旧人物。

handover 只允许：

同 generation
同 track_id
class一致
reference age合法
几何合法

的情况下进行。

---

# 38. SR warmup 也要改善

最新 debug：

RealESRGAN warmup ≈1977ms。

启动前约十几帧只能：

ROI-LANCZOS / BASE。

这是启动阶段问题，
不是周期性问题，

但可以改善体验。

考虑：

程序启动 rebuild 模式时立即 warmup

而不是等第一批 reference 后才进入稳定状态。

或者 HUD 明确：

SR WARMING

不要把启动阶段统计混入稳定期质量统计。

---

# 39. Debug 增强

最终每帧建议输出：

SEQ
VPTS

SPTS
STATE_DELTA
STATE_ARRIVAL_AGE
STATE_EXTRAP

GEN

TRACK
LOCAL_RGEN
STATE_RGEN
USED_RGEN

RPTS
AGE

REFREADY
USED

MODE
DROP_REASON

DXR
DYR
AREA_RATIO

CONTENT_REASON
MATCH
REG_X
REG_Y

PATCH_W
PATCH_H
MASK_W
MASK_H
VISIBLE_RATIO

SR_STATE
SR_LOOKUP

不要再出现：

DROP=CONTENT
MATCH=none

却无法判断为什么。

---

# 40. 修正 DROP_REASON=NONE 但 BASE 的情况

最新 debug 存在：

MODE=BASE-LANCZOS
RPTS=none
REFREADY=0
USED=0
DROP_REASON=NONE

这是 observability bug。

任何 BASE frame 必须拥有明确原因。

如果 STATE 存在但：

targets为空

记录：

EMPTY_STATE

如果没有可重建目标：

NO_TARGET

如果其他原因：

对应准确原因。

验收要求：

BASE + DROP_REASON=NONE
必须为0。

---

# 41. 新增回归测试：STATE one-frame lag

source：

6fps

PTS：

1000
1167
1334
...

对应STATE人为延迟一帧：

video=1167
latest state=1000

先测试：

playout=0

允许看到当前旧行为用于 baseline。

再测试：

playout≈170ms

当1167 STATE在buffer窗口内到达后：

1167 frame 应使用对应 STATE

不能：

BASE。

---

# 42. STATE late-arrival race 测试

顺序：

video frame N 到达

20ms后 STATE N 到达

PATCH已经可用

要求：

playout delay=50ms 时

frame N：

ROI

而不是：

先compose BASE
再因 immutable 永久保持BASE。

---

# 43. same-source-frame immutable 继续测试

buffer 后仍必须：

frame N 一旦真正 compose

不再被 STATE/PATCH/SR 修改。

playout buffer 的意义是：

延迟 compose 时间

不是：

允许 compose 后重写。

---

# 44. STATE extrapolation 测试

模拟：

当前STATE缺失

上一条历史STATE lag=167ms

同时有再前一条STATE可计算速度。

要求：

在允许的 bounded fallback 下：

外推得到当前bbox。

但如果：

track_id变化
class变化
lag过大
bbox速度异常

必须：

STATE DROP。

---

# 45. CONTENT stable-template 测试

准备同一 reference。

Case A：
SR cache miss

Case B：
SR cache hit

要求：

registration 使用相同raw template。

两个 case：

corrected位置应一致。

CONTENT accepted/rejected 结果应一致。

SR状态只影响：

最终贴图质量

不能影响：

registration identity decision。

---

# 46. CONTENT partial-outside 测试

reference crop 部分越出：

左
右
上
下

output canvas。

只要仍有足够 foreground 可见：

registration 应在 clipped region 工作。

不能因为：

patch尺寸略超canvas

直接：

CONTENT DROP。

完全不可见：

仍 fail closed。

---

# 47. CONTENT_LOW_SCORE 测试

真正错误人物/背景：

correlation score低于threshold。

必须：

DROP=CONTENT_LOW_SCORE

继续 fail closed。

不能为了提高ROI占比而接受错误贴图。

---

# 48. EMA continuity 测试

同 track：

RGEN=10
→11
→12
→13

bbox轻微变化。

要求：

EMA history 连续。

不能每次 RGEN变化重新初始化。

track_id改变后：

必须reset。

---

# 49. AGE/RGEN lag 测试

构造：

local latest refgen=12

selected STATE refgen=10

ref10 AGE=500

确认日志：

LOCAL_RGEN=12
STATE_RGEN=10
RGEN_LAG=2

便于明确：

这是 state lag

而不是 reference sender没有刷新。

---

# 50. 多目标测试

如果 max_targets >1：

至少测试2个人。

统计每 track：

reference interval
AGE max
SR cache
refresh count

任何一个持续目标都不应因为另一个目标：

长期 reference starvation。

如果发现 starvation：

再实现 EDF reference candidate scheduler。

---

# 51. 不允许的伪修复

禁止：

STATE_SYNC 100 → 500 后直接完成

reference hard age 450 →1000

关闭 content registration

minimum match score →0

关闭 SR

关闭 ROI

取消 same-source immutable

无限增加 PATCH 带宽

STATE 绕过100kbps rate limiter

每帧重复 compose 直到 STATE 到

通过显示旧人像掩盖 state mismatch

---

# 52. 建议修改文件

重点：

tools/live_h265_hud.py

tools/rebuild_receiver.py

tools/test_rebuild_protocol.py

必要时：

cpp/transport/rebuild_sender.cc
cpp/transport/rebuild_sender.h

RatePacer相关代码只在 telemetry证明需要时修改。

文档：

启动.md
协议.md
fix/分析相关文档

---

# 53. 实施优先级

P0-1
先增加最新 debug 自动解析器和细化 telemetry

P0-2
实现有界 rebuild playout buffer

P0-3
解决 state extrapolation 与100ms hard reject矛盾

P0-4
拆分 CONTENT drop subreason

P0-5
registration template 与 SR asset 解耦

P0-6
支持 partial/out-of-bounds registration clipping

P0-7
重新验证 AGE drop 是 STATE lag 还是 sender refresh

P1-1
bbox EMA key 去掉 reference_generation

P1-2
SR-ready two-phase handover

P1-3
Real-ESRGAN startup warmup优化

P2
只有数据证明后才修改：
多目标reference EDF
STATE packet scheduling priority

---

# 54. 真机 A/B 验证

使用完全相同场景：

A：
playout_delay=0

B：
playout_delay=建议值

各运行至少30秒。

统计：

BASE率
ROI-LANCZOS率
ROI-ESRGAN率

STATE DROP率
CONTENT DROP率
AGE DROP率

ESRGAN→BASE transition
BASE→ESRGAN transition

ESRGAN→LANCZOS
LANCZOS→ESRGAN

最长连续BASE

STATE delta p50/p95/max

reference age p50/p95/max

new RGEN first-frame SR hit

端到端新增 latency。

---

# 55. 第一阶段成功标准

修完 STATE synchronization 后：

连续的一帧 lag：

SPTS≈VPTS-167ms

不能再稳定产生：

BASE
BASE
BASE...

正常稳定链路下：

STATE DROP 应显著下降。

特别是：

连续 STATE DROP >2 source frames

应基本消失。

---

# 56. 第二阶段成功标准

修完 CONTENT registration 后：

满足：

AGE <450
geometry正常
reference存在
同track/refgen

的帧，

不应因：

SR/Lanczos模板差异

大量 CONTENT DROP。

CONTENT DROP 必须：

少量
可解释
拥有具体subreason。

---

# 57. 第三阶段成功标准

正常单人移动场景：

主要模式应接近：

ROI-ESRGAN
ROI-ESRGAN
ROI-ESRGAN
...

允许偶发：

ROI-LANCZOS

在SR未ready时fallback。

不应继续高频：

ROI-ESRGAN
BASE
ROI-ESRGAN
BASE

也不应：

ESRGAN
Lanczos
ESRGAN
Lanczos

形成稳定周期。

---

# 58. 最终结果必须报告

# Result

## 1. Baseline Debug Analysis

自动统计最新debug。

## 2. Revised Root Causes

分别证明：

STATE race
CONTENT failure
AGE
SR handover
EMA

各自贡献。

## 3. State Synchronization Fix

说明playout buffer与extrapolation。

## 4. Content Registration Fix

说明稳定template和clipping。

## 5. Reference Age Analysis

说明AGE到底由：
STATE lag
还是sender refresh
导致。

## 6. SR Handover

是否实施、效果如何。

## 7. Tests

逐条命令 + PASS/FAIL。

## 8. Before / After Metrics

表格：

BASE
Lanczos
ESRGAN
STATE
CONTENT
AGE
transitions
latency

## 9. CUDA Verification

真实Real-ESRGAN或明确未执行。

## 10. RK3588 Verification

真实板端或明确未执行。

## 11. Remaining Risks

多目标
track switch
网络jitter
带宽
CUDA latency等。

## 12. Git

git diff --check

并明确：

没有pull
没有fetch
没有push
没有覆盖用户本地改动。