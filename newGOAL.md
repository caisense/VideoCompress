# 环境

我用本机pc（172.20.10.2）接收，板端（172.20.10.4）发送，虚拟机（172.20.10.10）是交叉编译环境。

# 核心实现要求
- 发送端可以中途切换--rate-profile参数，无需像之前重新启动。考虑用合适的方式接受这个参数
- 接收端可以根据发送端切换的参数实时调整视频档位，也无需重新启动。考虑用合适的帧或标志位作为控制参数来控制接收端随着发送端调整。
- 完成后总结实现方案，更新readme和启动文档