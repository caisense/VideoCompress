/goal

# 彻底修复 VideoCompress rebuild 模式下目标人脸周期性模糊问题

# 0. 执行范围

本任务只针对当前本地工作区执行。

不要 pull、fetch、push、reset、checkout 远程代码，不要覆盖当前本地未提交修改。

开始前先执行：

git status
git diff

保护当前用户已有修改。

线上 GitHub 当前 main 仅用于理解现有实现，不作为覆盖本地代码的来源。
最终以本地当前工作区代码为唯一修改对象。

读取本目标后不要停止在“已读取目标文件”，必须继续完成：

代码分析
→ 修改
→ 新增测试
→ 执行测试
→ debug/统计验证
→ 文档更新
→ git diff --check

直到当前环境能够完成的工作全部完成。

---

# 1. 本轮问题已经确认的根因

不要再围绕“future PTS 符号写反”修改代码。

当前接收端逻辑：

content_age = video_pts - reference_pts

age > 450ms
→ expired

age < -100ms
→ future

这个符号方向是正确的。

现有测试也已经覆盖：

449ms → ROI
450ms → ROI
451ms → BASE

-100ms → ROI
-101ms → BASE

不得反转这个判断。

本轮 debug 中大量 AGE=167ms、334ms 的 reference 正常进入 ROI-ESRGAN，
说明“历史超过100ms被当成future清除”不是当前问题。

---

# 2. Debug 已确认的三个实际问题

本次 debug.txt 共记录约 389 个 source frame。

实际 mode 大致为：

BASE-LANCZOS：178
ROI-ESRGAN：137
ROI-LANCZOS：74

其中 BASE 帧大致可以拆分为：

AGE > 450ms：76 帧

AGE 在合法范围内但仍 BASE：86 帧

没有 reference / timing 信息：16 帧

AGE < -100ms future 导致 BASE：0 帧

所以本轮必须同时解决以下三个问题。

---

# 3. 根因 A：reference 实际刷新周期约 500/667ms，与 450ms hard age 冲突

当前发送端默认：

patch_soft_refresh_ms = 350
patch_hard_deadline_ms = 450
patch_refresh_guard_ms = 75

source fps = 6fps

6fps 的 source frame 间隔约：

166.7ms

当前 refresh 判断只能随着 source request 执行，因此：

0ms
167ms
333ms
500ms

soft threshold=350ms 时：

333ms 尚未满足
500ms 才第一次满足

结果是所谓 350ms refresh 实际大量变成约 500ms。

debug 中 reference generation 的相邻 RPTS 间隔：

多数约 498~504ms
其次约 663~671ms
中位数约 501ms

这和代码行为完全一致。

与此同时 receiver 在：

AGE >450ms

时严格回退 BASE。

因此形成典型周期：

AGE 167
ROI-ESRGAN

AGE 334
ROI-ESRGAN

AGE 500
BASE-LANCZOS

AGE 667
BASE-LANCZOS

下一 reference
ROI-ESRGAN

最终肉眼表现为：

清晰
→ 清晰
→ 模糊
→ 清晰
→ 清晰
→ 模糊

这是本轮 P0 问题。

---

# 4. Sender refresh 当前还有时间基准不一致问题

当前 RebuildSender 使用：

last_reference_ready_time_us

计算什么时候刷新下一 reference。

但 receiver 的 450ms 内容有效期是基于：

reference capture PTS

即：

current_video_pts - reference_capture_pts

两个时钟起点不同。

例如：

reference capture
0ms

JPEG/FEC/排队/发送完成
100ms

sender 从 ready 开始再等待 350ms

450ms

下一次 6fps process
约500ms

receiver 此时计算的是：

500 - 0 = 500ms

因此 reference 已经过期。

所以不能仅把：

350 → 250

当作最终修复。

必须修正 sender reference lifecycle，使 refresh 调度围绕：

“当前 reference 的 capture/content age”

而不是仅围绕：

“reference 在网络上 ready 后已经多久”。

---

# 5. P0 修改：重新设计 reference refresh scheduler

目标：

下一代 reference 应尽量在旧 reference 对 receiver 来说超过 450ms 前完成。

不能让：

soft refresh threshold

成为一个导致实际刷新晚于 hard content deadline 的下限。

当前：

return max(
    patch_soft_refresh_ms,
    hard_deadline - guard - delivery
)

需要重新审视。

在 6fps 离散调度环境下，算法必须考虑：

source frame scheduling quantum ≈ 167ms

estimated reference delivery time

queue delay

FEC/chunk transmission

450ms hard content deadline

guard

不要只根据连续时间公式计算 threshold，
因为真正执行 beginReference() 的机会只发生在 source frame request 到来时。

---

# 6. 推荐的 sender 判断思想

在每次 source request 到达时，对当前 track 判断：

current_reference_content_age_ms
+
estimated_reference_delivery_ms
+
scheduling / safety margin

是否已经接近：

patch_hard_deadline_ms

如果如果继续等到下一个 source request 很可能导致旧 reference 在下一可显示 source frame 过期，则现在立即启动新 reference。

核心目标：

不是：

“至少 hold 350ms 才刷新”

而是：

“不要错过在旧 reference 过期前准备下一代的最后机会”。

---

# 7. Sender 必须使用 capture-time 生命周期

已有：

last_reference_capture_time_us

和：

last_reference_ready_time_us

不要删除 telemetry。

但 refresh deadline 的核心 reference age 应以：

last_reference_capture_time_us

为主要时钟。

ready_time 可以用于：

最小驻留时间
统计
带宽节流
防止 generation 过度更新

但不能让它决定 content deadline。

接收端和发送端必须对：

reference lifetime

采用相同语义。

---

# 8. 不允许简单每帧都刷新 reference

必须继续满足 100kbps 链路预算。

禁止修成：

6fps 每帧一张新 PATCH

如果这会导致：

H265 被 PATCH 抢占
queue delay 上升
主码流延迟
P-frame 丢失
reference generation 过快导致 SR stale

需要在：

reference 时效
和
链路占用

之间平衡。

目标优先级：

避免周期 BASE gap
>
>极高 reference 更新频率。

---

# 9. 推荐 initial 行为目标

在单目标、6fps、正常 100kbps rebuild 配置下：

正常 reference generation 周期应尽量落到：

约 333ms

而不是稳定 500ms。

允许根据实际 delivery p95 动态调整。

不强制硬编码 333ms。

但最终必须满足：

典型情况下旧 reference 到 AGE≈500ms 时，
下一代 reference 已经存在并可使用，

从而避免：

AGE500 → BASE

这种稳定周期。

---

# 10. 增加 sender refresh telemetry

当前已有：

capture time
encode finish
queue enter
first packet
last packet
capture_to_send
p50
p95
blob bytes
chunk count

继续保留。

新增或补充方便直接判断 refresh 调度的数据，例如：

REFGEN
REF_CAPTURE_AGE
REF_READY_AGE
REFRESH_THRESHOLD
EST_DELIVERY
NEXT_DEADLINE
REFRESH_REASON

例如 debug：

REFRESH track=1
capture_age=332ms
ready_age=245ms
delivery_p95=72ms
hard=450ms
decision=START
reason=HARD_DEADLINE

这样真机才能判断 scheduler 是否正确。

---

# 11. 根因 B：_registered_crop() 的 absolute motion gate 过严

当前代码中：

center_dx > 0.25 * max(ref_width, cur_width)

或：

center_dy > 0.25 * max(ref_height, cur_height)

直接：

return None

同时 area_ratio 还要求：

0.70 <= area_ratio <= 1.40

问题是：

reference_bbox 是 reference 捕获时目标的位置，

current bbox 是当前目标的位置。

但 _registered_crop() 本身的职责恰恰就是：

把 reference crop 从旧 bbox 映射到当前 bbox。

所以：

“目标移动了”

本身不应该直接成为：

“reference 无效”

的理由。

---

# 12. Debug 对 registration 问题的直接证据

存在大量：

AGE=166/167ms
或
AGE=333/334ms

reference 完全没过期，

SR cache 已经 HIT，

但最终：

READY=BASE
MODE=BASE-LANCZOS
MATCH=none

例如典型：

新 reference：
AGE=0
ROI-LANCZOS

下一 frame：
AGE=166
CACHE hit
BASE-LANCZOS
MATCH=none

再下一 frame：
AGE=336
CACHE hit
BASE-LANCZOS
MATCH=none

这说明：

不是 reference 不存在
不是 AGE>450
不是 future
不是 ESRGAN 不存在

而是在进入 content registration 之前就被拒绝。

结合当前 render 路径：

_registered_crop()
→ None
→ registration_drops++
→ continue

而 MATCH 仍然 none，

高度指向：

_registered_crop() 前置 geometry gate。

---

# 13. P0 修改：重构 geometry registration gate

不要简单：

0.25 → 0.50

然后结束。

需要重新定义职责。

目标：

track/generation/reference_generation/PTS 已经证明这是当前目标的 reference 后，

目标在画面中的正常平移：

不应该因为绝对 center displacement 被直接拒绝。

_registered_crop() 应负责：

reference_bbox
→ current bbox

的空间映射。

正常 translation 是映射输入，
不是错误。

---

# 14. 推荐 geometry gate 新设计

保留：

非法 bbox
宽高<=0
极端 scale
明显异常 geometry
完全不可映射

等 fail-closed 条件。

但删除或大幅放宽：

相对于 reference bbox 尺寸的 25% absolute center movement gate。

推荐逻辑：

1. 根据 reference_bbox → current target bbox
   计算平移 + isotropic scale。

2. 生成 predicted crop。

3. 判断 projected crop 是否仍合理：
   - 尺寸有效
   - 非无限放大
   - 与画面有合理交集
   - scale ratio 不极端

4. 再交给 _content_register() 做内容级验证。

即：

geometry 用于预测位置

content matching 用于判断这张旧图是否仍和当前目标内容一致。

不要让：

“人在画面中走了一小段距离”

直接触发：

BASE。

---

# 15. area_ratio 门限也要重新验证

当前：

0.70 ~ 1.40

需要通过测试和真实 debug 判断是否过窄。

不要无条件删除 scale gate。

但必须区分：

正常 bbox 呼吸 / 透视变化
和
明显错误 track / bbox 崩坏。

可以设计配置，例如：

registration_min_area_ratio
registration_max_area_ratio

但不要为了追求 ROI duty cycle 把门限无限放宽。

核心仍然：

fail closed。

---

# 16. content_register 继续作为最终安全门

当前 _content_register()：

在 predicted location 附近搜索约 ±8px

并使用 matchTemplate，

最低 score 约 0.32。

这个机制继续保留。

如果目标：

有纹理
但相关度低

则仍应：

CONTENT_DROP
→ BASE

不能为了避免模糊，把明显错位 reference 强行贴上去。

---

# 17. 需要把 geometry drop 原因拆开

当前：

registration_drops

把多种情况混在一起。

必须增加详细原因统计：

GEOM_INVALID
GEOM_CENTER
GEOM_SCALE
GEOM_OUTSIDE
CONTENT_LOW_SCORE

如果移除 center gate 后 GEOM_CENTER 不再使用，可以保留兼容统计或删除。

最终 debug 至少能输出：

DROP=NONE
DROP=AGE
DROP=FUTURE
DROP=NOREF
DROP=STATE
DROP=GEN
DROP=GEOM
DROP=SCALE
DROP=CONTENT

不要再依靠：

MATCH=none

反推原因。

---

# 18. Debug 中 READY 字段也需要修正

当前 debug：

READY=LOCAL if refs_used else BASE

这个命名不准确。

refs_used 表示：

最终是否成功 blend 了 reference。

它不等于：

local reference 是否 ready。

因此可能出现：

reference 明明 local-ready
但 geometry drop
→ READY=BASE

会误导分析。

修改 debug。

建议改成：

REFREADY=1/0
USED=1/0

例如：

REFREADY=1
USED=0
DROP=GEOM

或者：

LOCAL=1
MODE=BASE-LANCZOS
DROP=CONTENT

必须能区分：

reference 不存在

和：

reference 存在但被某个 gate 拒绝。

---

# 19. 根因 C：新 reference 第一帧经常只有 Lanczos，下一帧才 ESRGAN

debug 中大量出现：

新 RGEN
AGE≈0
ROI-LANCZOS
SR MISS

下一 source frame
AGE≈167
ROI-ESRGAN
SR HIT

这是明显的周期清晰度变化。

Real-ESRGAN 本身并非完全跟不上。

问题是：

新 reference 到达时 SR 结果经常还没进入 cache。

---

# 20. 当前 future prefetch 设计需要修改

当前 _reference_gate_reason() 同时用于：

display eligibility

和：

SR compute eligibility。

如果：

reference 比当前 video PTS 超前超过100ms，

返回：

future

随后 prefetch：

defer_latest
SRWAIT FUT++

不立即执行 SR。

这对于显示是正确的：

未来 reference 不能提前画。

但对于纯 GPU 预计算不是必须的。

必须拆开：

COMPUTE SAFETY

和：

RENDER SAFETY。

---

# 21. P0 修改：future reference 可以提前计算，但不能提前显示

目标：

完整 PATCH 到达 PC 后，只要：

generation 合法
track 合法
reference_generation 合法
不是 stale
不是明显已经不可用

就允许 Real-ESRGAN 提前运行。

即使当前：

reference_pts - video_pts >100ms

也可以提前：

SR compute
→ SR cache

但 compositor 最终 render 时继续严格执行：

future >100ms
→ BASE / reject

核心原则：

未来图：

可以提前“算”

不能提前“画”。

---

# 22. 拆分 SR gate 与 render gate

不要继续让：

_reference_gate_reason()

一个函数决定全部事情。

推荐拆分或增加 mode：

_reference_compute_gate_reason()

_reference_render_gate_reason()

或者：

_reference_gate_reason(..., phase="compute/render")

要求：

Render gate：

generation
track
refgen
advertised generation
age <=450
future >=-100
geometry/content

全部严格。

SR compute gate：

generation
track
stale refgen
cache lifecycle

严格。

但是 future 可以更宽松。

---

# 23. SR compute 完成时也不要仅因“仍然 future”就丢弃结果

当前 _poll() 完成后再次调用：

_reference_gate_reason()

如果此时仍 future，

会把已算好的结果当 stale/drop。

这同样需要拆开。

SR result 是否可进入 cache：

主要判断：

generation 是否仍有效
track 是否仍有效
是否已经被 newer reference_generation 替代
是否已经真正过期 / 不再有未来使用价值

不应该因为：

当前视频还没追到该 reference

就删除 GPU 结果。

否则失去 prefetch 的意义。

---

# 24. 但 SR cache 结果不能提前 render

即使：

SR cache 已有 future reference

render selection 仍必须执行原有：

reference_pts - video_pts <=100ms

否则绝对禁止使用。

这个安全边界不得改变。

---

# 25. 保留当前 SR latest-only 机制

当前已经实现：

1 running
+
最多1 pending latest

继续保留。

不能改回无限 queue。

当前：

expired-before-submit
stale-after-compute
newer generation wins

这些正确机制继续保留。

不要重复推翻。

---

# 26. 保留当前 450ms hard age

不要因为 AGE500 大量发生就：

450ms → 700ms / 1000ms

这不是正确修复。

450ms 是内容时效要求。

真正应修：

sender 新 reference 要更早准备好。

禁止通过：

延长旧 reference 生命周期

掩盖 refresh scheduler 问题。

---

# 27. 保留 current future PTS sign

不得修改为反向。

当前正确语义：

age = video - reference

age >450
→ expired

age <-100
→ future

必须继续成立。

新增回归测试确保以后不再被错误修改。

---

# 28. 保留 PATCH local-ready

当前 complete PATCH 已经：

进入 reference_history
reference_generations
completed_reference_events

并可立即 prefetch。

不要恢复成：

必须等下一条 STATE ready

才认为 reference local-ready。

STATE 继续决定当前 source frame 对应哪个 refgen，
但不能重新否定本地已经完整收到的 PATCH。

---

# 29. 保留 same-source-frame immutable

当前已经正确实现：

同 source_sequence 再次 render：

直接返回 cached composed frame。

异步：

PATCH
STATE
SR

都只能影响下一 source frame。

这个设计必须保留。

不要为了让 ESRGAN 在当前帧“即时变清晰”而破坏它。

---

# 30. P0 Debug 升级

--rebuild-debug-timing

每个 source frame 至少输出：

SEQ
VPTS
SPTS
RPTS
AGE
GEN
RGEN

REFREADY
USED

MODE

DROP_REASON

MATCH

GEOM_DX_RATIO
GEOM_DY_RATIO
AREA_RATIO

SR_LOOKUP
SR_STATE

CACHE

例如：

SEQ=125
AGE=167
RGEN=31
REFREADY=1
USED=0
MODE=BASE-LANCZOS
DROP=GEOM_SCALE
DXR=0.08
DYR=0.12
AR=1.53
SR=HIT

这样下次真机日志能直接归因。

---

# 31. 统计每种 drop 的累计计数

HUD / snapshot 至少增加：

AGEDROP
FUTUREDROP
NOREFDROP
STATEDROP
GENDROP
GEOMDROP
SCALEDROP
MATCHDROP

已有计数可复用。

不要重复维护含义相同的 counters。

最终要求：

registration_drops

如果保留，

应能进一步拆分看到具体组成。

---

# 32. 增加 reference refresh 测试

测试 6fps 调度。

模拟：

frame time：

0
167
334
501
...

当前 reference capture at 0。

确保 scheduler 不会因为：

soft=350

直到501ms才开始下一 reference。

测试应证明：

在正常 estimated delivery 下，

应在 334ms 或更早的 source opportunity 触发 refresh。

不能等到：

501ms。

---

# 33. 增加 capture-time vs ready-time 测试

模拟：

capture=0ms

reference ready=100ms

当前 time=334ms

如果只按 ready age：

234ms

可能认为不用刷新。

但 receiver content age 已是：

334ms。

scheduler 必须基于 content deadline 正确判断是否需要开始下一代。

测试明确防止重新使用：

ready age 作为唯一 refresh age。

---

# 34. 增加 slow delivery 测试

模拟：

reference delivery p95 增大。

例如：

delivery=150ms

scheduler 应比网络快速时更早开始。

证明：

estimated delivery time

真正参与 deadline scheduling。

不要只是 telemetry。

---

# 35. 增加 bandwidth protection 测试

确保新的 refresh scheduler：

不会一个 source frame 连续创建多个 reference

不会在 pending_reference.active 时启动另一个 transfer

不会绕过 RatePacer

不会让 patch_packets_per_frame 超过配置

继续保持：

H265 / STATE / PATCH

共享物理链路预算。

---

# 36. 修改 registration 测试

当前已有：

large moved target
→ _registered_crop() is None

这一测试需要重新评估。

因为“大幅平移本身就是无效”的假设正是当前 bug 之一。

修改为至少两类：

A. Same track normal translation

reference bbox：
例如 x=50~150

current bbox：
明显平移到右侧

scale 接近1

应成功返回 translated crop。

不能仅因为 center displacement >25% fail。

B. Impossible geometry

例如：

面积扩大数倍
bbox非法
映射结果无效

仍然 fail closed。

---

# 37. 增加 moving-person regression test

模拟一个 track 连续移动：

frame 0:
bbox x=50

frame 1:
bbox x=70

frame 2:
bbox x=90

frame 3:
bbox x=110

reference generation 不变
AGE 均 <450ms

要求：

不能因为平移：

ROI → BASE → BASE

应尽量保持：

ROI
ROI
ROI
ROI

如果 content match 真正失败，
才允许 BASE。

---

# 38. 增加 geometry drop reason 测试

分别构造：

invalid bbox
extreme scale
content mismatch

必须得到不同：

DROP_REASON。

避免以后 debug 再只能看到：

MATCH=none。

---

# 39. 增加 future SR precompute 测试

场景：

video_pts=1000

reference_pts=1200

reference future=+200ms

Render：

必须拒绝。

但：

prefetch()

应允许 SR worker 计算并把结果缓存。

随后 video_pts 前进：

1150 或1200

满足 +100ms render gate 后：

下一 source frame 应直接：

ROI-ESRGAN

而不是：

ROI-LANCZOS
→ 下一帧 ESRGAN。

---

# 40. 增加 future result cache 测试

SR 在 reference 仍 future 时完成。

要求：

result 保留在 cache。

不能：

sr_stale++
future_drop
删除 result

仅当：

generation stale
refgen stale
track invalid
reference 已真正过期

时才废弃。

---

# 41. same-source-frame immutable 测试必须继续通过

保留当前测试：

source_sequence=100

第一次：
ROI-LANCZOS

中途 SR cache ready

第二次 source_sequence=100：

仍然：
ROI-LANCZOS

像素完全一致。

source_sequence=101：

才允许：
ROI-ESRGAN。

禁止本轮 SR prefetch 修改破坏这个测试。

---

# 42. 450ms / future 边界测试继续通过

必须继续：

449 → ROI
450 → ROI
451 → BASE

-100 → ROI
-101 → BASE

这两个测试属于永久 regression protection。

---

# 43. 新 reference 第一帧质量验收

修改前 debug 中经常：

new RGEN
ROI-LANCZOS

next frame
ROI-ESRGAN

修改后，在 PATCH 能够提前到达且 GPU 空闲时：

应尽量实现：

new RGEN 第一张可显示 source frame
直接 ROI-ESRGAN。

不能保证所有情况下100%，

但 `SRWAIT FUT` 不应再因为单纯 future >100ms 阻止正常 precompute。

需要统计：

new-reference-first-frame SR hit ratio。

---

# 44. 真机验收指标

重新运行相同场景至少20~30秒。

解析 debug log。

必须输出修改前后对比：

BASE-LANCZOS frame count

ROI-LANCZOS frame count

ROI-ESRGAN frame count

AGE>450 BASE count

valid-age BASE count

NOREF count

GEOM count

MATCH count

new RGEN first-frame SR hit rate

reference interval p50/p95/max

reference delivery p50/p95

SR latency p50/p95

SRWAIT FUT

SR STALE

---

# 45. 关键验收目标

目标不是简单：

AGEDROP=0

因为极端网络下 reference 仍可能合理过期。

目标是：

正常稳定单目标场景中，

周期性的：

AGE500
→ BASE

应该大幅减少或消失。

尤其不能继续出现稳定：

ROI
ROI
BASE
ROI
ROI
BASE

这种固定节奏。

---

# 46. Geometry 验收目标

在人物正常移动的情况下，

AGE<450
reference存在
refgen正确
SR HIT

不能因为 absolute bbox translation：

持续变成 BASE。

如果最终 BASE：

debug 必须明确告诉原因：

CONTENT
SCALE
STATE
NOREF
AGE
...

不能再是无法区分原因的：

MATCH=none。

---

# 47. SR 验收目标

在 GPU 性能足够时：

future PATCH 可提前计算。

保持：

future reference 不提前显示。

目标：

降低：

ROI-ESRGAN
→ 新RGEN ROI-LANCZOS
→ ROI-ESRGAN

这种每次换代时的周期清晰度脉冲。

---

# 48. 不允许使用这些伪修复

禁止：

把450ms改成700/1000ms

future gate改反

删除future render gate

关闭SR

关闭ROI

关闭registration全部安全检查

把geometry门限无限放宽

取消content match

每个6fps frame强制发新reference

绕过100kbps RatePacer

取消same-source-frame immutable

为了测试通过而改测试预期但不修逻辑

---

# 49. 推荐修改文件

重点检查并修改：

tools/rebuild_receiver.py

tools/live_h265_hud.py

tools/test_rebuild_protocol.py

cpp/transport/rebuild_sender.cc

cpp/transport/rebuild_sender.h

cpp/common/config.cc

cpp/common/config.h

以及对应 C++ sender tests / transport tests。

如果实际代码路径不同，
根据本地实现定位。

---

# 50. 执行顺序

1. git status / git diff

2. 阅读本地当前实现

3. 先确认本地代码是否和本目标描述一致

4. 先增强 DROP_REASON/debug telemetry

5. 写 regression tests 重现：
   500ms refresh
   valid-age geometry BASE
   future SR wait

6. 修 sender capture-time deadline scheduler

7. 修 registration translation gate

8. 拆分 SR compute/render future gate

9. 跑 Python protocol/composer tests

10. 跑 C++ sender tests

11. git diff --check

12. 如果有 CUDA：
    跑真实 Real-ESRGAN

13. 如果有板卡：
    跑相同真机场景生成新 debug

14. 用脚本自动统计新 debug

15. 对比修改前 debug.txt

16. 更新启动/协议/fix 文档

17. 最终审查 diff

---

# 51. Agent 必须实际分析 debug.txt

如果本地项目目录存在本次提供的 debug.txt，
请编写或临时运行一个解析脚本。

至少计算：

各 MODE 次数

AGE>450 BASE

AGE合法 BASE

future BASE

无 reference BASE

reference generation interval 分布

新 RGEN 首帧 mode

LANCZOS→ESRGAN transition 次数

不要仅肉眼浏览日志。

如果脚本只是临时分析工具，
任务完成后可以不提交。

---

# 52. 最终输出格式

# Result

## 1. Confirmed Root Causes

明确说明最终确认：

A. refresh deadline
B. geometry registration
C. SR future prefetch

分别是否成立。

## 2. Rejected Hypothesis

明确说明：

future PTS sign reversed

是否成立。

如果不成立，说明为什么没有修改。

## 3. Local Changes

逐文件说明修改内容。

## 4. Sender Refresh Model

说明：

旧算法
新算法
capture time / ready time 如何使用
6fps scheduling quantum 如何处理

## 5. Registration Model

说明：

删除/调整了哪些 gate
哪些 fail-closed 条件仍然保留
content match 如何工作

## 6. SR Compute vs Render Gate

说明：

future reference 什么情况下可compute
什么情况下可render

## 7. Tests

列出：

实际执行命令
PASS/FAIL
测试总数

## 8. Debug Comparison

修改前：

BASE
ROI-LANCZOS
ROI-ESRGAN
AGE DROP
valid-age BASE
reference interval

修改后对应数据。

## 9. CUDA Verification

实际执行或明确未执行。

## 10. RK3588 Verification

实际执行或明确未执行。

## 11. Remaining Risks

仍存在的：
带宽
多目标
快速运动
tracker ID switch
SR latency
等风险。

## 12. Recommended Run Command

给出最终本地启动命令。

## 13. Git Status

git diff --check 结果。

明确：

未执行 pull
未执行 fetch
未执行 push
未覆盖用户原有未提交修改。

---

# 53. 最终架构原则

本轮修复必须坚持：

1. PTS SIGN MUST REMAIN CORRECT

历史 reference 为正 AGE，
未来 reference 为负 AGE。

2. HARD AGE MUST REMAIN HARD

>450ms 不得继续显示。

3. REFRESH BEFORE EXPIRY

解决旧 reference 过期问题的方式是让新 reference 更早到，
不是让旧 reference 活更久。

4. MOTION IS NOT MISMATCH

同一 track 的正常空间移动不应被 absolute center gate 误杀。

5. CONTENT VALIDATES IDENTITY

几何负责预测位置，
内容匹配负责最终 fail-closed。

6. FUTURE MAY COMPUTE, NOT RENDER

未来 reference 可以提前做 ESRGAN，
但不能提前显示。

7. SOURCE FRAME IMMUTABLE

同一个 source frame 一旦 compose，
任何异步结果都只能影响下一 source frame。

8. BANDWIDTH REMAINS HARD

所有优化必须继续服从实际100kbps共享链路。

---

# 最终任务目标

在不放宽 450ms 内容时效、不破坏 future render gate、
不破坏 same-source-frame immutable、
不突破100kbps链路预算的前提下，

解决当前 debug 已确认的三种周期性质量退化：

1. reference 实际约500/667ms刷新导致的
   ROI-ESRGAN → BASE-LANCZOS

2. 正常目标移动被25% absolute center geometry gate误杀导致的
   ROI → BASE

3. 新 reference 换代时 SR 尚未准备好导致的
   ROI-ESRGAN → ROI-LANCZOS → ROI-ESRGAN

最终使正常单目标运动场景中的人脸重建保持连续稳定，
并让每一次 BASE fallback 都可以通过明确 DROP_REASON 定位。